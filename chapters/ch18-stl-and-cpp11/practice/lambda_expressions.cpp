/*
 * Medium Problem 3: Lambda Expressions
 * 
 * Create a vector of integers. Use lambda expressions with STL algorithms:
 * - Use for_each with a lambda to print each element
 * - Use transform with a lambda to square each element
 * - Use count_if with a lambda to count even numbers
 * Demonstrate capture clauses by capturing an external variable.
 */

#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

int main() {
    // TODO: Your code here
    
    return 0;
}
